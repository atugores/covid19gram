#!/bin/env python
# -*- coding: utf-8 -*-
import string
import configparser
from apscheduler.schedulers.asyncio import AsyncIOScheduler
import os
import re
import asyncio
from datetime import timedelta
from pyrogram import Client, filters, idle
from pyrogram.types import InlineKeyboardButton, InlineKeyboardMarkup, InputMediaPhoto, ReplyKeyboardMarkup, InlineQueryResultArticle, InputTextMessageContent, CallbackQuery
from pyrogram.errors import BadRequest
from covid19plot import COVID19Plot
from config.getdata import update_data, status_data
from config.countries import countries
from config.settings import DBHandler
import gettext
import redis
from uuid import uuid4
import logging

logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO)

config_file = "conf.ini"
config = configparser.ConfigParser()
config.read(config_file, encoding="utf-8")

admins = list(map(int, config["USER"]["admin"].split(',')))
app = Client(
    "covid19bot",
    bot_token=config["API"]["api_token"],
    api_id=config["API"]["API_ID"],
    api_hash=config["API"]["API_HASH"]
)
cplt = COVID19Plot()
all_regions = []
for scope in cplt.SCOPES:
    all_regions.extend([{'region': region, 'scope': scope} for region in cplt.get_regions(scope)])
all_regions.sort(key=lambda x: x['region'])

translations = {}
dbhd = DBHandler()
cache = redis.Redis(host='localhost', port=6379, db=0)
CACHE_PREFIX = 'COVID'
MAXTEXT = 3000

for language in cplt.LANGUAGES:
    translation = gettext.translation('messages', localedir='locales', languages=[language])
    translation.install()
    translations[language] = translation


async def exception_handle(user, e):
    if "USER_IS_BLOCKED" in str(e):
        logging.info(str(user) + ": Blocked the bot")
        return True
    elif "INPUT_USER_DEACTIVATED" in str(e):
        logging.info(str(user) + ": deactivated account")
        return True
    return False


def separa_qlsvl(text):
    separat = []
    max = MAXTEXT
    number = (len(text) - 1) // max + 2
    j = 0
    for i in range(1, number):
        separat.append((text[j * max: i * max]).rstrip(" \n"))
        j = i
    return separat


def separa_espais(text):
    paraules = text.split(" ")
    separat = []
    info = ""
    llarg = 0
    for paraula in paraules:
        if len(paraula) >= MAXTEXT:
            aux = separa_qlsvl(info + paraula)
            separat = separat + aux[: -1]
            info = (aux[-1] + " ").lstrip(" \n")
            llarg = len(info)
        elif llarg + len(paraula) >= MAXTEXT:
            separat.append(info.rstrip(" \n"))
            llarg = len(paraula)
            info = (paraula + " ").lstrip(" \n")
        else:
            llarg = llarg + len(paraula)
            info = (info + paraula + " ").lstrip(" \n")
    separat.append(info.rstrip(" "))
    return separat


def separa(text):
    linies = text.splitlines()
    separat = []
    info = ""
    llarg = 0
    for linea in linies:
        if len(linea) >= MAXTEXT:
            aux = separa_espais(info + linea)
            separat = separat + aux[: -1]
            info = (aux[-1] + "\n").lstrip("\n")
            llarg = len(info)
        elif llarg + len(linea) >= MAXTEXT:
            separat.append(info.rstrip("\n"))
            llarg = len(linea)
            info = (linea + "\n").lstrip("\n")
        else:
            llarg = llarg + len(linea)
            info = (info + linea + "\n").lstrip("\n")

    separat.append(info.rstrip(" \n"))

    return separat


async def envia(client, chat_id, text):
    linies = []
    if len(text) >= MAXTEXT:
        linies = separa(text)
        m = ()
        for linea in linies:
            m = await client.send_message(chat_id, linea, parse_mode="markdown")
        return(m)
    else:
        return await client.send_message(chat_id, text, parse_mode="markdown")


def normal(s):
    replacements = (
        ("á", "a"),
        ("é", "e"),
        ("í", "i"),
        ("ó", "o"),
        ("ú", "u"),
        ("à", "a"),
        ("è", "e"),
        ("ï", "i"),
        ("ò", "o"),
        ("ü", "u"),
        ("î", "i"),
    )
    for a, b in replacements:
        s = s.replace(a, b).replace(a.upper(), b.upper())
    return s


def cerca(paraula, language="en"):
    _ = translations[language].gettext
    cerca = normal(paraula).lower()
    resultats = []
    for region in all_regions:
        if normal(_(region['region'])).lower().find(cerca) > -1:
            resultats.append(region)
    return resultats


def region_sort_key(language=None):
    def sort_function(elem):
        if elem.startswith("total-"):
            return "ÑÑÑ"
        if language:
            _ = translations[language].gettext
            return normal(_(elem))
        return normal(elem)
    return sort_function


def get_label(scope='world', language='en'):
    _ = translations[language].gettext
    label = '\n' + _('__Generated by [COVID19gram](t.me/COVID19gram_bot)__') + '\n'
    if scope == 'world':
        label += _('__Data source from__') + ' __[CSSEGISandData/COVID-19](https://github.com/CSSEGISandData/COVID-19)__'
    elif scope == 'spain':
        label += _('__Data source from__') + ' __[Datadista](https://github.com/datadista/datasets/)__'
    elif scope == 'italy':
        label += _('__Data source from__') + ' __[Ministero della Salute (Italia)](https://github.com/pcm-dpc/COVID-19)__'
    elif scope == 'france':
        label += _('__Data source from__') + ' __[OpenCOVID19-fr](https://github.com/opencovid19-fr)__'
    elif scope == 'austria':
        label += _('__Data source from__') + ' __[covid-data-austria](https://github.com/Daniel-Breuss/covid-data-austria)__'
    else:
        label += _('__Data source from__') + ' __[Proyecto COVID-19](https://covid19tracking.narrativa.com/)__'
    return label


def get_caption(region, scope, plot_type="daily_cases", language="en", is_scope=False):
    # scp = cplt.get_region_scope(region)
    # if plot_type == 'recovered' and region == f"total-{scope}" and scp in cplt.AGES:
    #     return cplt.get_plot_caption(plot_type, region, language)
    if is_scope:
        plot_caption = cplt.get_scope_plot_caption(plot_type=plot_type, scope=scope, language=language)
        return plot_caption

    _ = translations[language].gettext
    flaged_region = region
    if region in countries:
        flaged_region = countries[region]['flag'] + _(region)
    else:
        flaged_region = _(region)
    title = ""
    footer = ""
    if plot_type == "daily_cases":
        title = _('Cases increase at {region}').format(region=flaged_region)
    elif plot_type == "daily_hospitalized":
        title = _('Hospitalization evolution at {region}').format(region=flaged_region)
    elif plot_type == "active_recovered_deceased":
        title = _('Active cases, recovered and deceased at {region}').format(region=flaged_region)
        if region in cplt.get_regions('italy') or region == 'total-france' or region in cplt.get_regions('austria'):
            title = _('Active cases, hospitalized, recovered and deceased at {region}').format(region=_(region))
        elif region in cplt.get_regions('france'):
            title = _('Currently hospitalized, recovered and deceased at {region}').format(region=_(region))
    elif plot_type == "active":
        title = _('Active cases at {region}').format(region=flaged_region)
        if region in cplt.get_regions('france') and region != "total-france":
            title = _('Active hospitalizations at {region}').format(region=flaged_region)
    elif plot_type == "cases":
        title = _('Cumulative cases at {region}').format(region=flaged_region)
        if region in cplt.get_regions('france') and region != "total-france":
            title = _('Active hospitalizations at {region}').format(region=flaged_region)
    elif plot_type == "reproduction_rate":
        title = _('Reproduction rate at {region}').format(region=flaged_region)
        scp = cplt.get_region_scope(region)
        if region == f"total-{scp}" and scp in cplt.AGES:
            title = _("Cases (Deaths) by age:")
    elif plot_type == "daily_deceased":
        title = _('Deaths evolution at {region}').format(region=flaged_region)
    elif plot_type == "hospitalized":
        title = _('Active hospitalizations at {region}').format(region=_(region))
    if plot_type == "summary":
        title = _('COVID-19 status at {region}').format(region=flaged_region)
        plot_caption = cplt.get_summary(region=region, language=language)
        # footer = "\n__" + _("More information and plots at") + " @COVID19gram_bot__\n"
        footer = ""
    else:
        plot_caption = cplt.get_plot_caption(plot_type=plot_type, region=region, scope=scope, language=language)
    return f'**{title}**\n\n{plot_caption}{footer}'


def botons(plot_type="daily_cases", regio="total-world", scope="world", language="en", method=None, acum_regions_key=None):
    _ = translations[language].gettext
    ibt = []
    l_botns = []
    botonets = []
    result_all = []
    acum_regions = []
    regions = cplt.get_regions(scope)
    regions.sort(key=region_sort_key(language))

    if acum_regions_key:
        cache_acum_regions = cache.get(acum_regions_key)
        if cache_acum_regions:
            acum_regions = cache_acum_regions.decode("utf-8").split('_')

    for chart in list(string.ascii_lowercase):
        result = [i for i in regions if normal(_(i)[0]).lower() == chart and i != "total-" + scope and i not in acum_regions]
        result_all.extend(result)
        if len(result_all) > 7 or chart == "z":
            for item in result_all:
                flag = ""
                if item in countries:
                    flag = countries[item]['flag']
                cb = ""
                if method:
                    cb = method + "_" + scope + "_" + item
                else:
                    cb = item + "_" + scope
                cb = cb + "_" + plot_type.replace('_', '-')
                ibt.append(InlineKeyboardButton(flag + _(item), callback_data=cb))
            botonets = [ibt[i * 3:(i + 1) * 3] for i in range((len(ibt) // 3) + 1)]
            cb = "back_" + scope
            if method:
                cb = cb + '_' + method
                if acum_regions_key:
                    cb = cb + '_' + acum_regions_key.replace('_', '-')
            botonets.extend([[InlineKeyboardButton(_("⬅️Back"), callback_data=cb)]])
            btns = InlineKeyboardMarkup(botonets)
            l_botns.append(btns)
            ibt = []
            botonets = []
            result_all = []
    return l_botns


def b_alphabet(scope, plot_type="daily_cases", regio="total-world", method=None, acum_regions_key=None, acum_regions=[], language="en"):
    _ = translations[language].gettext
    ibt = []
    botonets = []
    ordre = 0
    text = ""
    len_all = 0
    s_char = ""
    regions = cplt.get_regions(scope)

    if len(regions) <= 20:
        return b_regions(scope, plot_type, method, acum_regions, language)

    regions.sort(key=region_sort_key(language))
    nnames = {
        'world': _("🌐Global"),
        'spain': _("🇪🇸Spain"),
        'italy': _("🇮🇹Italy"),
        'france': _("🇫🇷France"),
        'austria': _("🇦🇹Austria"),
        'argentina': _('🇦🇷Argentina'),
        'australia': _('🇦🇺Australia'),
        'brazil': _('🇧🇷Brazil'),
        'canada': _('🇨🇦Canada'),
        'chile': _('🇨🇱Chile'),
        'china': _('🇨🇳China'),
        'colombia': _('🇨🇴Colombia'),
        'germany': _('🇩🇪Germany'),
        'india': _('🇮🇳India'),
        'mexico': _('🇲🇽Mexico'),
        'portugal': _('🇵🇹Portugal'),
        'us': _('🇺🇲US'),
        'unitedkingdom': _('🇬🇧United Kingdom'),
    }

    cb = "total-" + scope
    if method:
        cb = method + "_" + scope + "_" + cb
        if acum_regions_key:
            cb = cb + '_' + acum_regions_key.replace('_', '-')
    else:
        cb += "_" + scope + "_" + plot_type.replace('_', '-')
    if "total-" + scope not in acum_regions:
        ibt.append(InlineKeyboardButton(nnames[scope], callback_data=cb))

    for chart in list(string.ascii_lowercase):
        result = [i for i in regions if normal(_(i)[0]).lower() == chart and i != "total-world"]
        if len(result) > 0:
            len_all += len(result)
            if len_all > 7 or chart == "z":
                if s_char == "":
                    text = chart.upper()
                else:
                    text = s_char + "-" + chart.upper()
                s_char = ""
                len_all = 0
                cb = "alph_" + scope + "_" + str(ordre)
                if method:
                    cb = cb + "_" + method
                    if acum_regions_key:
                        cb = cb + '_' + acum_regions_key.replace('_', '-')
                ibt.append(InlineKeyboardButton(text, callback_data=cb))
                ordre += 1
            elif len(result) > 0:
                if s_char == "":
                    s_char = chart.upper()
    botonets = [ibt[i * 3:(i + 1) * 3] for i in range((len(ibt) // 3) + 1)]
    if method == 'compare':
        botonets.append([
            InlineKeyboardButton("✅ " + _("Done"), callback_data="compare_" + scope + "_finish_cases-normalized")
        ])
    btns = InlineKeyboardMarkup(botonets)
    return btns


def b_single(user_id, plot_type="daily_cases", region="total-world", scope='world', language="en"):
    _ = translations[language].gettext
    logging.info(f"REGION: {region}")
    subregion = None
    if region in ['Argentina', 'Australia', 'Brazil', 'Canada', 'Chile', 'China', 'Colombia', 'Germany', 'India', 'Mexico', 'Portugal', 'US', 'United Kingdom', 'Spain', 'France', 'Austria', 'Italy']:
        subregion = region.lower().replace(' ', '')
    fav_emoji = "🖤"
    fav_label = "fav"
    p_type = plot_type.replace('_', '-')
    recovered_emoji = "👩‍👦‍👦"
    if dbhd.is_subscribed(user_id, region, scope):
        fav_emoji = "💛"
        fav_label = "unfav"
    # scope = cplt.get_region_scope(region)
    if region == f"total-{scope}" and scope in cplt.AGES:
        recovered_emoji = "🚻"
    bttns = [
        InlineKeyboardButton("🦠", callback_data="s_" + scope + "_" + region + "_daily-cases"),
        InlineKeyboardButton("📊", callback_data="s_" + scope + "_" + region + "_active-recovered-deceased"),
        InlineKeyboardButton("📈", callback_data="s_" + scope + "_" + region + "_cases"),
        InlineKeyboardButton(recovered_emoji, callback_data="s_" + scope + "_" + region + "_reproduction-rate"),
        InlineKeyboardButton("❌", callback_data="s_" + scope + "_" + region + "_daily-deceased")
    ]

    if subregion:
        bttns.append(InlineKeyboardButton("🗺", callback_data="do_" + subregion))

    bttns.append(InlineKeyboardButton(fav_emoji, callback_data=fav_label + "_" + scope + "_" + region + "_" + p_type))

    buttons = [bttns]
    if region.startswith("total-"):
        buttons.extend([[
            InlineKeyboardButton("🦠🗺", callback_data="scope_" + region + "_cases"),
            InlineKeyboardButton("🦠🆕", callback_data="scope_" + region + "_increase-cases-normalized-heatmap"),
            InlineKeyboardButton("🦠🖇", callback_data="scope_" + region + "_acum14-cases-normalized-heatmap")],
            [
            InlineKeyboardButton("❌", callback_data="scope_" + region + "_deceased-normalized"),
            InlineKeyboardButton("❌🖇", callback_data="scope_" + region + "_acum14-deceased-normalized-heatmap"),
        ]])

    buttons.append([
        InlineKeyboardButton("⬇️ " + _("Send all plots"), callback_data="sendall_" + scope + "_" + region),
        InlineKeyboardButton("📊 " + _("Add region to compare"), callback_data="compare_" + scope + "_" + region),
    ])
    btns = InlineKeyboardMarkup(buttons)
    return btns


def b_compare(scope='world', language="en"):
    _ = translations[language].gettext
    buttons = [[
        InlineKeyboardButton("🦠", callback_data="compare_" + scope + "_finish_cases-normalized"),
        InlineKeyboardButton("📊", callback_data="compare_" + scope + "_finish_cases"),
        InlineKeyboardButton("🦠🖇", callback_data="compare_" + scope + "_finish_acum14-cases-normalized"),
        InlineKeyboardButton("🗂", callback_data="compare_" + scope + "_finish_cases-logarithmic"),
        # InlineKeyboardButton("✅", callback_data="s_" + region + "_reproduction-rate"),
        InlineKeyboardButton("❌", callback_data="compare_" + scope + "_finish_deceased-normalized"),
    ]]

    # buttons.append([
    #     InlineKeyboardButton("⬇️ " + _("Send all plots"), callback_data="sendall_" + region),
    # ])
    btns = InlineKeyboardMarkup(buttons)
    return btns


def b_find(search, plot_type="daily_cases", language="en"):
    _ = translations[language].gettext
    taula = cerca(search, language)
    ibt = []
    l_botns = []
    botonets = []
    pageSize = 18
    max = len(taula) // pageSize

    if len(taula) % pageSize != 0:
        max = len(taula) // pageSize + 1

    for pag in range(max):
        for item in taula[pag * pageSize:(pag + 1) * pageSize]:
            flag = ""
            scope = _(f'{item["scope"].capitalize()}')
            # country name fix
            if item['scope'] == 'unitedkingdom':
                scope = _('United Kingdom')
            if item['scope'] == 'us':
                scope = _('US')
            scope = "(" + scope + ")"
            if item['region'] in countries:
                flag = countries[item['region']]['flag']
                scope = ""
            ibt.append(InlineKeyboardButton(flag + _(item['region']) + scope, callback_data=item['region'] + "_" + item['scope'] + "_" + plot_type.replace('_', '-')))
        botonets = [ibt[i * 3:(i + 1) * 3] for i in range((len(ibt) // 3) + 1)]
        if pag == 0 and pag != max - 1:
            botonets.extend([[InlineKeyboardButton(">>", callback_data="f_" + str(pag + 1) + "_" + search)]])
        elif pag == max - 1 and pag != 0:
            botonets.extend([[InlineKeyboardButton("<<", callback_data="f_" + str(pag - 1) + "_" + search)]])
        elif pag > 0 and pag < max - 1:
            botonets.extend([[
                InlineKeyboardButton("<<", callback_data="f_" + str(pag - 1) + "_" + search),
                InlineKeyboardButton(">>", callback_data="f_" + str(pag + 1) + "_" + search)]])
        btns = InlineKeyboardMarkup(botonets)
        l_botns.append(btns)
        ibt = []
        botonets = []
    return l_botns


async def b_fav(user, plot_type="daily_cases", language="en"):
    _ = translations[language].gettext
    regions = await dbhd.get_subscriptions(user)
    ibt = []
    l_botns = []
    botonets = []
    pageSize = 18
    max = len(regions) // pageSize

    if len(regions) % pageSize != 0:
        max = len(regions) // pageSize + 1

    for pag in range(max):
        for item in regions[pag * pageSize:(pag + 1) * pageSize]:
            flag = ""
            if item['region'] in countries:
                flag = countries[item['region']]['flag']
            ibt.append(InlineKeyboardButton(flag + _(item['region']), callback_data=item['region'] + "_" + item['scope'] + "_" + plot_type.replace('_', '-')))
        botonets = [ibt[i * 3:(i + 1) * 3] for i in range((len(ibt) // 3) + 1)]
        if pag == 0 and pag != max - 1:
            botonets.extend([[InlineKeyboardButton(">>", callback_data="subs_" + str(pag + 1))]])
        elif pag == max - 1 and pag != 0:
            botonets.extend([[InlineKeyboardButton("<<", callback_data="subs_" + str(pag - 1))]])
        elif pag > 0 and pag < max - 1:
            botonets.extend([[
                InlineKeyboardButton("<<", callback_data="subs_" + str(pag - 1)),
                InlineKeyboardButton(">>", callback_data="subs_" + str(pag + 1))]])
        btns = InlineKeyboardMarkup(botonets)
        l_botns.append(btns)
        ibt = []
        botonets = []
    return l_botns


def b_regions(scope, plot_type="daily_cases", method=None, acum_regions=[], language="en"):
    _ = translations[language].gettext
    ibt = []
    botonets = []
    regions = cplt.get_regions(scope)
    regions.sort(key=region_sort_key(language))
    for item in regions:
        if item in acum_regions:
            continue
        cb = ""
        if method:
            cb = method + "_" + scope + "_" + item
        else:
            cb = item + "_" + scope
        cb = cb + "_" + plot_type.replace('_', '-')
        ibt.append(InlineKeyboardButton(_(item), callback_data=cb))
    botonets = [ibt[i * 3:(i + 1) * 3] for i in range((len(ibt) // 3) + 1)]
    if method == 'compare':
        botonets.append([
            InlineKeyboardButton("✅ " + _("Done"), callback_data="compare_" + scope + "_finish_cases-normalized")
        ])
    btns = InlineKeyboardMarkup(botonets)
    return btns


def b_conf(user_id, language="en", btn_show={'lng': 'on', 'ntf': 'on', 'shw': 'on'}):
    _ = translations[language].gettext
    names = {
        'gl': _("🌐Global"),
        'es': _("🇪🇸Spain"),
        'it': _("🇮🇹Italy"),
        'fr': _("🇫🇷France"),
        'at': _("🇦🇹Austria"),
        'ar': _('🇦🇷Argentina'),
        'au': _('🇦🇺Australia'),
        'br': _('🇧🇷Brazil'),
        'ca': _('🇨🇦Canada'),
        'cl': _('🇨🇱Chile'),
        'cn': _('🇨🇳China'),
        'co': _('🇨🇴Colombia'),
        'de': _('🇩🇪Germany'),
        'in': _('🇮🇳India'),
        'mx': _('🇲🇽Mexico'),
        'pt': _('🇵🇹Portugal'),
        'us': _('🇺🇲US'),
        'gb': _('🇬🇧United Kingdom'),
    }

    nnames = {
        'world': _("🌐Global"),
        'spain': _("🇪🇸Spain"),
        'italy': _("🇮🇹Italy"),
        'france': _("🇫🇷France"),
        'austria': _("🇦🇹Austria"),
        'argentina': _('🇦🇷Argentina'),
        'australia': _('🇦🇺Australia'),
        'brazil': _('🇧🇷Brazil'),
        'canada': _('🇨🇦Canada'),
        'chile': _('🇨🇱Chile'),
        'china': _('🇨🇳China'),
        'colombia': _('🇨🇴Colombia'),
        'germany': _('🇩🇪Germany'),
        'india': _('🇮🇳India'),
        'mexico': _('🇲🇽Mexico'),
        'portugal': _('🇵🇹Portugal'),
        'us': _('🇺🇲US'),
        'unitedkingdom': _('🇬🇧United Kingdom'),
    }
    lang_dicc = {'en': '⚫️', 'es': '⚫️', 'ca': '⚫️', 'it': '⚫️'}
    lang_dicc[language] = '🔵'
    emj_btn = {}
    btn_status = {}
    emj_notf = {}
    cb_estat = {}
    for bt in dbhd.SCOPES:
        emj_btn[bt] = '⚫️'
        btn_status[bt] = 'on'

    for button in dbhd.get_buttons(user_id):
        emj_btn[button] = '🔵'
        btn_status[button] = 'off'

    for nt in dbhd.NSCOPES:
        emj_notf[nt] = '⚫️'
        cb_estat[nt] = 'on'

    for nt in dbhd.get_notifications(user_id):
        if nt != 'none':
            emj_notf[nt] = '🔵'
            cb_estat[nt] = 'off'

    stts = {'on': '🔺', 'of': '🔻'}

    lng_show = 'on'
    if btn_show['lng'] == 'on':
        lng_show = 'of'

    ntf_show = 'on'
    if btn_show['ntf'] == 'on':
        ntf_show = 'of'

    shw_show = 'on'
    if btn_show['shw'] == 'on':
        shw_show = 'of'

    buttons = [
        [
            InlineKeyboardButton(_("Choose Language") + ": " + stts[lng_show], callback_data="cnf_" + lng_show + "_" + btn_show['ntf'] + "_" + btn_show['shw'])
        ]
    ]
    if btn_show['lng'] == 'of':
        buttons += [
            [
                InlineKeyboardButton(lang_dicc['en'] + " " + "English", callback_data="lang_en_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw']),
                InlineKeyboardButton(lang_dicc['ca'] + " " + "Català", callback_data="lang_ca_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw'])
            ],
            [
                InlineKeyboardButton(lang_dicc['es'] + " " + "Español", callback_data="lang_es_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw']),
                InlineKeyboardButton(lang_dicc['it'] + " " + "Italiano", callback_data="lang_it_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw']),
            ]
        ]

    buttons += [
        [
            InlineKeyboardButton(_("Notify me of updates to") + ": " + stts[ntf_show], callback_data="cnf_" + btn_show['lng'] + "_" + ntf_show + "_" + btn_show['shw'])
        ]
    ]
    if btn_show['ntf'] == 'of':
        nbt = []
        for scope in dbhd.NSCOPES:
            nbt.append(InlineKeyboardButton(emj_notf[scope] + " " + nnames[scope], callback_data="notf_" + scope + "_" + cb_estat[scope] + "_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw']))
        buttons += [nbt[i * 3:(i + 1) * 3] for i in range((len(nbt) // 3) + 1)]

    buttons += [
        [
            InlineKeyboardButton(_("Show buttons for:") + " " + stts[shw_show], callback_data="cnf_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + shw_show)
        ]]
    if btn_show['shw'] == 'of':
        nbt = []
        for scope in dbhd.SCOPES:
            nbt.append(InlineKeyboardButton(emj_btn[scope] + " " + names[scope], callback_data="button_" + scope + "_" + btn_status[scope] + "_" + btn_show['lng'] + "_" + btn_show['ntf'] + "_" + btn_show['shw']))
        buttons += [nbt[i * 3:(i + 1) * 3] for i in range((len(nbt) // 3) + 1)]
    btns = InlineKeyboardMarkup(buttons)
    return btns


def b_start(user_id, language="en"):
    _ = translations[language].gettext
    names = {
        'gl': _("🌐Global"),
        'es': _("🇪🇸Spain"),
        'it': _("🇮🇹Italy"),
        'fr': _("🇫🇷France"),
        'at': _("🇦🇹Austria"),
        'ar': _('🇦🇷Argentina'),
        'au': _('🇦🇺Australia'),
        'br': _('🇧🇷Brazil'),
        'ca': _('🇨🇦Canada'),
        'cl': _('🇨🇱Chile'),
        'cn': _('🇨🇳China'),
        'co': _('🇨🇴Colombia'),
        'de': _('🇩🇪Germany'),
        'in': _('🇮🇳India'),
        'mx': _('🇲🇽Mexico'),
        'pt': _('🇵🇹Portugal'),
        'us': _('🇺🇲US'),
        'gb': _('🇬🇧United Kingdom'),
    }
    buttons = dbhd.get_buttons(user_id)
    scps = []
    for button in buttons:
        scps.append(names[button])

    scopes = [scps[i * 4:(i + 1) * 4] for i in range((len(scps) // 4) + 1)]

    rep_markup = ReplyKeyboardMarkup(scopes + [[_("⚙️Conf."), _("❓About"), _("💛FAVs")]], resize_keyboard=True)
    return rep_markup


async def set_language(user_id, language):
    await dbhd.set_language(user_id, language)


async def get_language(user):
    language = await dbhd.get_language(user.id)
    if language != 'None':
        return language
    elif user.language_code and user.language_code in cplt.LANGUAGES:
        await set_language(user.id, user.language_code)
        return user.language_code
    else:
        await set_language(user.id, 'en')
        return 'en'


async def send_photo(client, chat, photo, caption="", reply_markup=[]):
    if await dbhd.has_image_hash(photo):
        flname = await dbhd.get_image_hash(photo)
        message = await client.send_photo(chat, photo=flname, caption=caption, reply_markup=reply_markup)
    else:
        flname = photo
        message = await client.send_photo(chat, photo=flname, caption=caption, reply_markup=reply_markup)
        hash = message.photo.file_id
        await dbhd.set_image_hash(hash, flname)
    return message


async def edit_message_media(client, chat, mid, photo, caption, reply_markup):
    message = None
    if await dbhd.has_image_hash(photo):
        message = flname = await dbhd.get_image_hash(photo)
        await client.edit_message_media(chat, mid, InputMediaPhoto(media=flname, caption=caption), reply_markup=reply_markup)
    else:
        flname = photo
        message = await client.edit_message_media(chat, mid, InputMediaPhoto(media=flname, caption=caption), reply_markup=reply_markup)
        hash = message.photo.file_id
        await dbhd.set_image_hash(hash, flname)
    return message


async def show_region(client, chat, plot_type="daily_cases", region="total-world", scope="world", language='en', is_scope=False, simple=False):
    _ = translations[language].gettext
    btns = []
    if not simple:
        btns = b_single(chat, plot_type=plot_type, region=region, scope=scope, language=language)
    if is_scope:
        flname = cplt.generate_scope_plot(plot_type=plot_type, scope=scope, language=language)
        caption = get_caption(region, plot_type=plot_type, language=language, scope=True)
    else:
        if scope == 'france' and plot_type == 'daily_cases' and region != 'total-france':
            plot_type = 'daily_hospitalized'
        flname = cplt.generate_plot(plot_type=plot_type, region=region, scope=scope, language=language)
        caption = get_caption(region, scope=scope, plot_type=plot_type, language=language)
    try:
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
    except BadRequest as e:
        if str(e).find("IMAGE_PROCESS_FAILED") > -1:
            os.remove(flname)
            if is_scope:
                flname = cplt.generate_scope_plot(plot_type=plot_type, scope=scope, language=language)
            else:
                flname = cplt.generate_plot(plot_type=plot_type, scope=scope, region=region, language=language)
            await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
        elif str(e).find("MESSAGE_NOT_MODIFIED") > -1:
            logging.info("Error: " + str(e))
    except Exception as err:
        logging.info("Unexpected error:" + str(type(err)) + " - " + str(err))
        raise


async def send_regions(client, chat, region="Total", scope="world", language='en', is_scope=False):
    _ = translations[language].gettext
    media = []
    flnames = []
    if is_scope:
        for plot_type in cplt.BUTTON_PLOT_TYPES:
            flname = cplt.generate_plot(plot_type=plot_type, region=region, scope=scope, language=language)
            flnames.append(flname)
            if await dbhd.has_image_hash(flname):
                flname = await dbhd.get_image_hash(flname)
            media.append(InputMediaPhoto(
                flname,
                caption=get_caption(region, scope, plot_type=plot_type, language=language)
            ))
        for plot_type in cplt.BUTTON_SCOPE_PLOT_TYPES:
            flname = cplt.generate_scope_plot(plot_type=plot_type, scope=scope, language=language)
            flnames.append(flname)
            if await dbhd.has_image_hash(flname):
                flname = await dbhd.get_image_hash(flname)
            media.append(InputMediaPhoto(
                flname,
                caption=get_caption(region, scope, plot_type=plot_type, language=language, scope=True)
            ))
    else:
        for plot_type in cplt.BUTTON_PLOT_TYPES:
            if scope == 'france' and plot_type == 'daily_cases' and region != 'total-france':
                plot_type = 'daily_hospitalized'
            flname = cplt.generate_plot(plot_type=plot_type, region=region, scope=scope, language=language)
            flnames.append(flname)
            if await dbhd.has_image_hash(flname):
                flname = await dbhd.get_image_hash(flname)
            media.append(InputMediaPhoto(
                flname,
                caption=get_caption(region, scope, plot_type=plot_type, language=language)
            ))
    try:
        messages = await client.send_media_group(chat, media=media)
        media_index = 0
        for msg in messages:
            if not await dbhd.has_image_filename(msg.photo.file_id):
                hash = msg.photo.file_id
                flname = flnames[media_index]
                await dbhd.set_image_hash(hash, flname)
            media_index += 1
    except BadRequest as e:
        if str(e).find("IMAGE_PROCESS_FAILED") > -1:
            for photo in media:
                os.remove(photo.media)
        elif str(e).find("MESSAGE_NOT_MODIFIED") > -1:
            logging.info("Error: " + str(e))
    except Exception as err:
        logging.info("Unexpected error:" + str(type(err)) + " - " + str(err))
        raise


async def edit_region(client, chat, mid, plot_type="daily_cases", region="Total", scope='spain', language="en", is_scope=False, compare=False):
    _ = translations[language].gettext
    btns = b_single(chat, plot_type=plot_type, region=region, scope=scope, language=language)

    if is_scope:
        # scope = region.replace('total-', '')
        flname = cplt.generate_scope_plot(plot_type=plot_type, scope=scope, language=language)
        caption = get_caption(region, scope, plot_type=plot_type, language=language, is_scope=True)
    elif compare:
        regions = cache.get(f"{CACHE_PREFIX}_compare_{chat}_{mid}")
        region_scope = scope
        if not regions:
            regions = []
        else:
            regions = regions.decode("utf-8").split('_')
            region_scope = scope
        if region_scope == 'france' and 'cases' in plot_type:
            plot_type = plot_type.replace('cases', 'hospitalized')
        flname = cplt.generate_multiregion_plot(plot_type=plot_type, regions=regions, scope=scope, language=language)
        caption = _("Comparison of cases in ") + ', '.join([_(region) for region in regions])
        btns = b_compare(scope, language)
    else:
        region_scope = scope
        if region_scope == 'france' and plot_type == 'daily_cases' and region != 'total-france':
            plot_type = 'daily_hospitalized'
        flname = cplt.generate_plot(plot_type=plot_type, region=region, scope=scope, language=language)
        caption = get_caption(region, plot_type=plot_type, scope=scope, language=language)
    try:
        await edit_message_media(client, chat, mid, flname, caption=caption, reply_markup=btns)
    except BadRequest as e:
        if str(e).find("IMAGE_PROCESS_FAILED") > -1:
            os.remove(flname)
            if is_scope:
                flname = cplt.generate_scope_plot(plot_type=plot_type, scope=scope, language=language)
            else:
                flname = cplt.generate_plot(plot_type=plot_type, region=region, scope=scope, language=language)
            await edit_message_media(client, chat, mid, flname, caption=caption, reply_markup=btns)
        elif str(e).find("MESSAGE_NOT_MODIFIED") > -1:
            logging.info("Error: " + str(e))
    except Exception as err:
        logging.info("Unexpected error:" + str(type(err)) + " - " + str(err))
        raise


async def send_notifications():
    updated = update_data()
    _ = translations['en'].gettext
    text = {
        'world': _('🌐Global data updated'),
        'spain': _('🇪🇸Spain data updated'),
        'italy': _('🇮🇹Italy data updated'),
        'france': _('🇫🇷France data updated'),
        'austria': _('🇦🇹Austria data updated'),
        'argentina': _('🇦🇷Argentina data updated'),
        'australia': _('🇦🇺Australia data updated'),
        'brazil': _('🇧🇷Brazil data updated'),
        'canada': _('🇨🇦Canada data updated'),
        'chile': _('🇨🇱Chile data updated'),
        'china': _('🇨🇳China data updated'),
        'colombia': _('🇨🇴Colombia data updated'),
        'germany': _('🇩🇪Germany data updated'),
        'india': _('🇮🇳India data updated'),
        'mexico': _('🇲🇽Mexico data updated'),
        'portugal': _('🇵🇹Portugal data updated'),
        'us': _('🇺🇲US data updated'),
        'unitedkingdom': _('🇬🇧United Kingdom data updated'),
    }
    for scope in cplt.SCOPES:
        if scope != 'france' and updated[scope]:
            for user_id in await dbhd.get_users_scope(scope):
                # await asyncio.sleep(1)
                language = await get_language(await app.get_users(user_id))
                _ = translations[language].gettext
                try:
                    await app.send_message(user_id, _(text[scope]))
                except Exception as e:
                    await exception_handle(user_id, e)


async def DoBot(comm, param, client, message, language="en", **kwargs):
    _ = translations[language].gettext
    chat = None
    user = None
    print(type(message))
    if type(message) == CallbackQuery:
        user = message.from_user
        chat = message.message.chat.id
    else:
        user = message.from_user.id
        chat = message.chat.id
    md_param = ""
    if 'md_param' in kwargs and kwargs['md_param'] != "":
        md_param = kwargs['md_param']
    if comm == "start":
        language = await get_language(message.from_user)
        _ = translations[language].gettext
        rep_markup = b_start(user, language)
        await client.send_message(chat, _("⚙️Main Menu"), reply_markup=rep_markup)
    elif comm == "italy":
        btns = b_regions('italy', language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope="italy", language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
    elif comm == "austria":
        btns = b_regions('austria', language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope="austria", language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
    elif comm == "france":
        btns = b_regions('france', language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope="france", language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
    elif comm == "spain":
        btns = b_regions('spain', language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope="spain", language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
        # await client.send_message(chat, caption, reply_markup=btns)
    elif comm == "world":
        btns = b_alphabet(scope="world", language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope="world", language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
        # await client.send_message(chat, caption, reply_markup=btns)
    elif comm in ['argentina', 'australia', 'brazil', 'canada', 'chile', 'china', 'colombia', 'germany', 'india', 'mexico', 'portugal', 'us', 'unitedkingdom']:
        btns = b_alphabet(comm, language=language)
        caption = _("Choose a Region")
        flname = cplt.generate_scope_plot(plot_type='map', scope=comm, language=language)
        await send_photo(client, chat, photo=flname, caption=caption, reply_markup=btns)
    elif comm == "countries":
        text = _("**List of Countries with regions**") + "\n\n"
        for country in ['argentina', 'australia', 'austria', 'brazil', 'canada', 'chile', 'china', 'colombia', 'france', 'germany', 'india', 'italy', 'mexico', 'portugal', 'spain', 'us', 'unitedkingdom']:
            text += f"/{country}\n"
        await client.send_message(chat, text)
    elif comm == "clean" and user in admins:
        await client.send_message(chat, "Cleanig images cache.")
        filelist = [f for f in os.listdir("images") if f.endswith(".png")]
        await dbhd.clean_hashes()
        for f in filelist:
            os.remove(os.path.join("images", f))
        await client.send_message(chat, "Cleaned.")
    elif comm == "update" and user in admins:
        await client.send_message(chat, "Updating data.")
        await send_notifications()
        await client.send_message(chat, "Data Updated.")
    elif comm == "status" and user in admins:
        text = status_data()
        text += "\n" + await dbhd.status_users()
        text += "\n" + await dbhd.status_notifications()
        text += "\n" + await dbhd.status_files()
        await envia(client, chat, text)
    elif comm == 'bc' and user in admins:
        ilkb = InlineKeyboardMarkup([
            [
                InlineKeyboardButton("Send Cat", callback_data="bc_ca"),
                InlineKeyboardButton("Send 🇪🇸", callback_data="bc_es"),
                InlineKeyboardButton("Send 🇬🇧", callback_data="bc_en"),
            ],
            [
                InlineKeyboardButton("Send 🇮🇹", callback_data="bc_it"),
                InlineKeyboardButton("Send 🌐", callback_data="bc_all"),
                InlineKeyboardButton("Cancel", callback_data="bc_none"),
            ]
        ])
        await client.send_message(chat, md_param, parse_mode=None, disable_web_page_preview=True)
        await client.send_message(chat, md_param, reply_markup=ilkb, disable_web_page_preview=True)
    elif comm == "find":
        if len(param) > 0:
            resultats = cerca(param, language=language)
            if len(resultats) == 0:
                rep_markup = b_start(user, language)
                await client.send_message(chat, _('No results for `{param}`').format(param=param), reply_markup=rep_markup)
            elif len(resultats) == 1:
                await show_region(client, chat, region=resultats[0]['region'], scope=resultats[0]['scope'])
            else:
                btns = b_find(param, language=language)[0]
                caption = f'Search Results for `{param}`'
                await client.send_message(chat, caption, reply_markup=btns)
    elif comm == "favs":
        lbtns = await b_fav(user, language=language)
        if len(lbtns) == 0:
            await client.send_message(chat, _("You have no subscription").format(param=param))
        else:
            btns = lbtns[0]
            caption = _("**Your subscriptions:**")
            await client.send_message(chat, caption, reply_markup=btns)

    elif comm == "about":
        about = _("**Chart Buttons**") + "\n"
        about += "🦠 - __" + _("Case increase") + ".__\n"
        about += "📊 - __" + _("Active cases, recovered and deceased") + ".__\n"
        about += "📈 - __" + _("Cumulative cases") + ".__\n"
        about += "👩‍👦‍👦 - __" + _("Reproduction Rate") + ".__\n"
        about += "❌ - __" + _("Daily deaths evolution") + ".__\n"
        about += "⬇️ - __" + _("Send all plots as an album") + ".__\n"
        about += "📊  - __" + _("Add region to compare") + ".__\n"
        about += '\n'
        about += "**" + _("Additional buttons. Top 20s for 🌐Global and detailed Spain scopes") + ":**\n"
        about += "🦠🗺 - __" + _("Active cases per region") + ".__\n"
        about += "🦠% - __" + _("Cases rate per 100K inhabitants") + ".__\n"
        about += "📈🆕 - __" + _("New cases rate per 100K inhabitants") + ".__\n"
        about += "❌% - __" + _("Deceased rate per 100K inhabitants") + ".__\n"
        about += "❌🆕 - __" + _("New deceased rate per 100K inhabitants") + ".__\n"
        about += '\n'

        about += _("**Data Sources**") + "\n"
        about += "🗂" + _('__Spain data source from__') + ' __[Datadista](https://github.com/datadista/datasets/)__\n'
        about += "🗂" + _('__World data source from__') + ' __[JHU CSSE](https://github.com/CSSEGISandData/COVID-19)__, '
        about += _('__transformed to JSON by__') + ' __[github.com/pomber](https://github.com/pomber/covid19)__\n'
        about += "🗂" + _('__Italy data source from__') + ' __[Ministero della Salute (Italia)](https://github.com/pcm-dpc/COVID-19)__\n'
        about += "🗂" + _('__France data source from__') + ' __[OpenCOVID19-fr](https://opencovid19.fr)__\n'
        about += "🗂" + _('__Austria data source from__') + ' __[covid-data-austria](https://github.com/Daniel-Breuss/covid-data-austria)__\n'
        about += "🗂" + _('__other countries data source from__') + ' __[Proyecto COVID-19](https://covid19tracking.narrativa.com/)__\n'
        about += '\n'
        about += _("**Contact**") + '\n'
        about += _("You can contact us using") + " [@C19G_feedbackbot](t.me/C19G_feedbackbot)" + "\n"

        about += _("**Code availability**") + "\n"
        about += _('Source code available soon.') + '\n'
        about += '\n＿＿＿＿＿＿＿＿＿＿＿＿＿'

        rep_markup = b_start(user, language)
        await client.send_message(chat, about, disable_web_page_preview=True, reply_markup=rep_markup)


@app.on_message(filters.text)
async def g_request(client, message):
    chat = message.chat.id
    user_id = message.from_user.id
    language = await get_language(message.from_user)
    _ = translations[language].gettext
    cntry_scope = {
        _("🌐Global"): 'world',
        _("🇪🇸Spain"): 'spain',
        _('🇮🇹Italy'): 'italy',
        _('🇫🇷France'): 'france',
        _('🇦🇹Austria'): 'austria',
        _('🇦🇷Argentina'): 'argentina',
        _('🇦🇺Australia'): 'australia',
        _('🇧🇷Brazil'): 'brazil',
        _('🇨🇦Canada'): 'canada',
        _('🇨🇱Chile'): 'chile',
        _('🇨🇳China'): 'china',
        _('🇨🇴Colombia'): 'colombia',
        _('🇩🇪Germany'): 'germany',
        _('🇮🇳India'): 'india',
        _('🇲🇽Mexico'): 'mexico',
        _('🇵🇹Portugal'): 'portugal',
        _('🇺🇲US'): 'us',
        _('🇬🇧United Kingdom'): 'unitedkingdom',
    }
    if message.text.startswith('/'):
        comm = message.text.split()[0].strip('/')
        param = ""
        if re.match('^/' + comm + ' .+', message.text):
            param = re.search('^/' + comm + ' (.+)', message.text).group(1)
        md_param = message.text.markdown.replace('/' + comm, '', 1)
        await DoBot(comm, param, client, message, language, md_param=md_param)
    elif message.text in cntry_scope:
        await DoBot(cntry_scope[message.text], "", client, message, language)
    elif message.text == _("⚙️Conf."):
        btns = b_conf(user_id, language)
        await client.send_message(chat, _("⚙️ **Configuration:**"), reply_markup=btns)
    elif message.text == _("❓About"):
        await DoBot("about", "", client, message, language)
    elif message.text == _("💛FAVs"):
        await DoBot("favs", "", client, message, language)
    else:
        param = message.text
        resultats = cerca(param, language)
        if len(resultats) == 0:
            await client.send_message(chat, _('No results for `{param}`').format(param=param))
        elif len(resultats) == 1:
            await show_region(client, chat, region=resultats[0]['region'], scope=resultats[0]['scope'], language=language)
        else:
            btns = b_find(param, language=language)[0]
            caption = _('Search results for `{param}`').format(param=param)
            await client.send_message(chat, caption, reply_markup=btns)


@app.on_callback_query()
async def answer(client, callback_query):
    user = callback_query.from_user
    user_id = callback_query.from_user.id
    chat = callback_query.message.chat.id
    mid = callback_query.message.message_id
    params = callback_query.data.split("_")
    comm = params[0]
    scope = params[1]
    language = await get_language(user)
    _ = translations[language].gettext
    if comm == "pag":
        logging.info("****** entra a pag ****")
        pag = int(params[1])
        btns = botons(scope=scope)[pag]
        caption = _("Choose a Region")
        await client.edit_message_text(chat, mid, caption, reply_markup=btns)

    elif comm == "do":
        region = params[2]
        if scope == 'void':
            scope = cplt.get_region_scope(region)
        await DoBot(region, "", client, callback_query, language)

    elif comm == "bc" and user.id in admins:
        lang = params[2]
        if lang in cplt.LANGUAGES or lang == "all":
            for user_id in await dbhd.get_users_lang(lang):
                # await asyncio.sleep(1)
                try:
                    language = await get_language(await client.get_users(user_id))
                    _ = translations[language].gettext
                    rep_markup = b_start(user_id, language)
                    await client.send_message(user_id, _("**Announcement:**") + "\n\n" + callback_query.message.text.markdown, disable_web_page_preview=True, reply_markup=rep_markup, disable_notification=True)
                except Exception as e:
                    await exception_handle(user_id, e)
            await client.send_message(user.id, f'The message has been send to `{lang}`.')
        else:
            await client.send_message(user.id, "Canceled.")
        await callback_query.message.delete()

    elif comm == "back":
        method = None
        if len(params) > 2:
            method = params[2]
        acum_regions_key = None
        if len(params) > 3:
            acum_regions_key = params[3].replace('-', '_')
        btns = b_alphabet(scope=scope, language=language, method=method, acum_regions_key=acum_regions_key)
        if not method:
            caption = _("Choose a Region")
            await client.edit_message_text(chat, mid, caption, reply_markup=btns)
        else:
            await client.edit_message_reply_markup(chat, mid, reply_markup=btns)

    elif comm == "s":
        region = params[2]
        if scope == 'void':
            scope = cplt.get_region_scope(region)
        plot_type = params[3].replace('-', '_')
        if region in [r['region'] for r in all_regions]:
            await edit_region(client, chat, mid, plot_type, region, scope, language=language)

    elif comm == "sendall":
        region = params[2]
        await client.send_message(chat, _("Generating all plots, it may take some time."))
        if region in [r['region'] for r in all_regions]:
            if region.startswith("total-"):
                await send_regions(client, chat, region=region, scope=scope, language=language, is_scope=True)
            else:
                await send_regions(client, chat, region=region, scope=scope, language=language)

    elif comm == "compare":
        region = params[2]
        if scope == 'void':
            scope = cplt.get_region_scope(region)
        if region == 'finish':
            plot_type = params[3].replace('-', '_')
            await edit_region(client, chat, mid, scope=scope, plot_type=plot_type, language=language, compare=True)
            await client.answer_callback_query(callback_query.id)
            return
        cache_key = f"{CACHE_PREFIX}_{comm}_{chat}_{mid}"
        regions = cache.get(cache_key)
        if not regions:
            regions = []
        else:
            regions = regions.decode("utf-8").split('_')
        regions.append(region)
        cache.setex(
            cache_key,
            timedelta(minutes=60),
            value='_'.join(regions))
        if len(regions) > 8:
            plot_type = 'cases_normalized'
            await edit_region(client, chat, mid, scope=scope, plot_type=plot_type, language=language, compare=True)
            await client.answer_callback_query(callback_query.id)
            return
        btns = b_alphabet(scope, method='compare', acum_regions_key=cache_key, acum_regions=regions, language=language)
        text = _("Choose a Region to compare with ") + ', '.join([_(region) for region in regions])
        await client.edit_message_text(chat, mid, text, reply_markup=btns)

    elif comm == "scope":
        region = params[2]
        if scope == 'void':
            scope = cplt.get_region_scope(region)
        plot_type = params[3].replace('-', '_')
        if region.startswith("total-"):
            await edit_region(client, chat, mid, plot_type, region, language=language, is_scope=True)

    elif comm == "lang":
        language = params[1]
        btn_show = {'lng': params[2], 'ntf': params[3], 'shw': params[4]}
        _ = translations[language].gettext
        await set_language(user.id, language)
        rep_markup = b_start(user.id, language)
        await client.send_message(chat, _("Your language is now English"), reply_markup=rep_markup, disable_notification=True)
        rep_markup = b_conf(user.id, language, btn_show)
        await client.edit_message_text(chat, mid, _("⚙️ **Configuration:**"), reply_markup=rep_markup)

    elif comm == "button":
        button = params[1]
        status = params[2]
        btn_show = {'lng': params[3], 'ntf': params[4], 'shw': params[5]}
        if await dbhd.set_button(user.id, button, status):
            rep_markup = b_start(user.id, language)
            await client.send_message(chat, _("Buttons updated"), reply_markup=rep_markup, disable_notification=True)
            rep_markup = b_conf(user.id, language, btn_show)
            await client.edit_message_text(chat, mid, _("⚙️ **Configuration:**"), reply_markup=rep_markup)
        else:
            await client.send_message(chat, _("You must select at least one button"), disable_notification=True)

    elif comm == "notf":
        scope = params[1]
        status = params[2]
        btn_show = {'lng': params[3], 'ntf': params[4], 'shw': params[5]}
        await dbhd.set_notification(user.id, scope, status)
        rep_markup = b_conf(user.id, language, btn_show)
        await client.edit_message_text(chat, mid, _("⚙️ **Configuration:**"), reply_markup=rep_markup)

    elif comm == "cnf":
        btn_show = {'lng': params[1], 'ntf': params[2], 'shw': params[3]}
        rep_markup = b_conf(user.id, language, btn_show)
        await client.edit_message_text(chat, mid, _("⚙️ **Configuration:**"), reply_markup=rep_markup)

    elif comm == "alph":
        pag = int(params[2])
        method = None
        if len(params) > 3:
            method = params[3]
        acum_regions_key = None
        if len(params) > 4:
            acum_regions_key = params[4].replace('-', '_')
        btns = []
        btns = botons(scope=scope, language=language, method=method, acum_regions_key=acum_regions_key)[pag]
        if not method:
            caption = _("Choose a Region")
            await client.edit_message_text(chat, mid, caption, reply_markup=btns)
        else:
            await client.edit_message_reply_markup(chat, mid, reply_markup=btns)

    elif comm == "f":
        pag = int(params[2])
        param = params[3]
        btns = b_find(param, language=language)[pag]
        caption = _('Search results for `{param}`').format(param=param)
        await client.edit_message_text(chat, mid, caption, reply_markup=btns)

    elif comm == "subs":
        pag = int(params[2])
        user_id = user.id
        lbtns = await b_fav(user_id, language=language)
        btns = lbtns[pag]
        caption = _("**Your subscriptions:**")
        await client.edit_message_text(chat, mid, caption, reply_markup=btns)

    elif comm == "fav":
        user_id = user.id
        region = params[2]
        plot_type = params[3].replace('-', '_')
        is_scope = False
        await dbhd.add_subscription(user_id, region, scope)
        if plot_type in cplt.SCOPE_PLOT_TYPES:
            is_scope = True
        await edit_region(client, chat, mid, plot_type, region, scope, language=language, is_scope=is_scope)

    elif comm == "unfav":
        user_id = user.id
        region = params[2]
        plot_type = params[3].replace('-', '_')
        is_scope = False
        await dbhd.remove_subscription(user_id, region, scope)
        if plot_type in cplt.SCOPE_PLOT_TYPES:
            is_scope = True
        await edit_region(client, chat, mid, plot_type, region, scope, language=language, is_scope=is_scope)

    elif comm in [r['region'] for r in all_regions]:
        region = comm
        if scope == 'void':
            scope = cplt.get_region_scope(region)
        plot_type = params[2].replace('-', '_')
        await show_region(client, chat, plot_type, region, scope, language=language)

    elif comm == "blank":
        logging.info('blank')

    await client.answer_callback_query(callback_query.id)


@app.on_inline_query()
async def answer_inline(client, inline_query):
    if len(inline_query.query) > 6:
        await inline_query.answer(
            results=[],
            is_personal=True,
            cache_time=20
        )
        return

    rslts = []
    resultats = []
    language = await get_language(inline_query.from_user)
    _ = translations[language].gettext

    if len(inline_query.query) < 1:
        regions = await dbhd.get_subscriptions(inline_query.from_user.id)
        if len(regions) > 0:
            resultats = regions[0:40]
        else:
            await inline_query.answer(
                results=[],
                is_personal=True,
                cache_time=20
            )
            return
    else:
        resultats = cerca(inline_query.query, language)
        resultats = resultats[0:40]

    for result in resultats:
        text = get_caption(result, plot_type="summary", language=language)
        flag = ""
        if result in countries:
            flag = countries[result]['flag']

        inlansw = InlineQueryResultArticle(
            id=uuid4(),
            title=flag + _(result),
            input_message_content=InputTextMessageContent(text),
        )
        rslts.append(inlansw)

    await inline_query.answer(
        results=rslts,
        is_personal=True,
        cache_time=20
    )


async def main():
    scheduler = AsyncIOScheduler()
    scheduler.add_job(send_notifications, "interval", hours=1)
    scheduler.start()
    await app.start()
    logging.info("Started")
    await idle()

if __name__ == "__main__":
    loop = asyncio.get_event_loop()
    loop.run_until_complete(main())
